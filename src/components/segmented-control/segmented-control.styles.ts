import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    height: 50,
  },
  activeBox: {
    position: 'absolute',
    borderRadius: 10,
    elevation: 3,
    height: '100%',
  },
});
