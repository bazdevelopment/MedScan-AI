import React from 'react';

import { Text } from '@/ui';

const GenerateReportScreen = ({ collectedData }) => {
  return <Text>Report will be displayed here</Text>;
};

export default GenerateReportScreen;
